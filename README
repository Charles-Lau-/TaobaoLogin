A taobao login project, which contains both Idea-Jave version and Python version
=======================================
Defects:
	For every user, the encrypted password and corresponding UA value must be catched in the browser

	If checkcode is required, it must be solved manually.

